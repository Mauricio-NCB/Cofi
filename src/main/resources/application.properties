# --------------- Configuración de BD ----------------

spring.datasource.url=jdbc:mysql://localhost:3306/website
spring.datasource.username=root
spring.datasource.password=!3Age@rootUCM

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

spring.application.name=main

# --------------- Configuración de JWT ----------------

# Clave secreta para firmar los tokens JWT
jwt.secret=mi_clave_secreta

# Valor en milisegundos de access token (30 minutos)
jwt.expiration=1800000

# Valor en milisegundos de refresh token (7 dias)
jwt.refreshExpiration=604800000